export class D3CapacityPlanSprint {
  /**
   * D3CapacityPlanSprint draws everything having to do with a sprint
   * @param record
   * @param chart
   */
  constructor (record, chart) {
    let self = this;
    
    self.record = record;
    self.parent = chart;
  }
  
  update(){
  
  }
  
  parseData(){
  
  }
}
