import './contributor_commitment_level.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.ContributorCommitmentLevel.helpers({
});

/**
 * Template Event Handlers
 */
Template.ContributorCommitmentLevel.events({});

/**
 * Template Created
 */
Template.ContributorCommitmentLevel.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.ContributorCommitmentLevel.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.ContributorCommitmentLevel.onDestroyed(() => {
  
});
