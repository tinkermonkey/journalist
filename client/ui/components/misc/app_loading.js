import './app_loading.html';
import './app_loading.css';

import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.AppLoading.helpers({});

/**
 * Template Event Handlers
 */
Template.AppLoading.events({});

/**
 * Template Created
 */
Template.AppLoading.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.AppLoading.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.AppLoading.onDestroyed(() => {
  
});
