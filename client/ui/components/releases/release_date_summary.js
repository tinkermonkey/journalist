import './release_date_summary.html';
import './release_date_summary.css';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.ReleaseDateSummary.helpers({});

/**
 * Template Event Handlers
 */
Template.ReleaseDateSummary.events({});

/**
 * Template Created
 */
Template.ReleaseDateSummary.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.ReleaseDateSummary.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.ReleaseDateSummary.onDestroyed(() => {
  
});
