import './status_menu_item.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.StatusMenuItem.helpers({});

/**
 * Template Event Handlers
 */
Template.StatusMenuItem.events({});

/**
 * Template Created
 */
Template.StatusMenuItem.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.StatusMenuItem.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.StatusMenuItem.onDestroyed(() => {
  
});
