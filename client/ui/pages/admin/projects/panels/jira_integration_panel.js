import './jira_integration_panel.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.JiraIntegrationPanel.helpers({});

/**
 * Template Event Handlers
 */
Template.JiraIntegrationPanel.events({});

/**
 * Template Created
 */
Template.JiraIntegrationPanel.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.JiraIntegrationPanel.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.JiraIntegrationPanel.onDestroyed(() => {
  
});
