import './confluence_integration_panel.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.ConfluenceIntegrationPanel.helpers({});

/**
 * Template Event Handlers
 */
Template.ConfluenceIntegrationPanel.events({});

/**
 * Template Created
 */
Template.ConfluenceIntegrationPanel.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.ConfluenceIntegrationPanel.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.ConfluenceIntegrationPanel.onDestroyed(() => {
  
});
