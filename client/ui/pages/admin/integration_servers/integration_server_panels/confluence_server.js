import './confluence_server.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.ConfluenceServer.helpers({});

/**
 * Template Event Handlers
 */
Template.ConfluenceServer.events({});

/**
 * Template Created
 */
Template.ConfluenceServer.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.ConfluenceServer.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.ConfluenceServer.onDestroyed(() => {
  
});
