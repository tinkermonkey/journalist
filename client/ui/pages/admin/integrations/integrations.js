import './integrations.html';
import { Template } from 'meteor/templating';
import './integration_servers';
import './integration_import_functions';
import './integration_display_templates';
import './integration_calculated_fields';
import './integration_status_maps';

/**
 * Template Helpers
 */
Template.Integrations.helpers({});

/**
 * Template Event Handlers
 */
Template.Integrations.events({});

/**
 * Template Created
 */
Template.Integrations.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.Integrations.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.Integrations.onDestroyed(() => {
  
});
