import './display_template_list.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.DisplayTemplateList.helpers({});

/**
 * Template Event Handlers
 */
Template.DisplayTemplateList.events({});

/**
 * Template Created
 */
Template.DisplayTemplateList.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.DisplayTemplateList.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.DisplayTemplateList.onDestroyed(() => {
  
});
