import './admin_stats_teams.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.AdminStatsTeams.helpers({});

/**
 * Template Event Handlers
 */
Template.AdminStatsTeams.events({});

/**
 * Template Created
 */
Template.AdminStatsTeams.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.AdminStatsTeams.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.AdminStatsTeams.onDestroyed(() => {
  
});
