import './default_backlog_team_report.html';
import { Template } from 'meteor/templating';
import './backlog_item_resource_requirements';

/**
 * Template Helpers
 */
Template.DefaultBacklogTeamReport.helpers({});

/**
 * Template Event Handlers
 */
Template.DefaultBacklogTeamReport.events({});

/**
 * Template Created
 */
Template.DefaultBacklogTeamReport.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.DefaultBacklogTeamReport.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.DefaultBacklogTeamReport.onDestroyed(() => {
  
});
