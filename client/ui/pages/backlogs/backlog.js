import './backlog.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.Backlog.helpers({});

/**
 * Template Event Handlers
 */
Template.Backlog.events({});

/**
 * Template Created
 */
Template.Backlog.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.Backlog.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.Backlog.onDestroyed(() => {
  
});
